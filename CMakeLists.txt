cmake_minimum_required(VERSION 3.20)

project(rp2040_doom_jaszczurhal NONE)

set(APP_NAME "rp2040_doom")
set(ARDUINO_CLI "arduino-cli" CACHE STRING "arduino-cli executable")
set(ARDUINO_FQBN "rp2040:rp2040:rpipico:usbstack=tinyusb" CACHE STRING "Arduino fully qualified board name")
set(ARDUINO_SKETCHBOOK "" CACHE PATH "Arduino sketchbook path")
set(ARDUINO_UPLOAD_PORT "" CACHE STRING "Arduino upload port")
set(ARDUINO_VERBOSE OFF CACHE BOOL "Enable verbose Arduino CLI output")
set(JH_USB_MANUFACTURER "" CACHE STRING "USB manufacturer string passed to Arduino-Pico")
set(JH_USB_PRODUCT "" CACHE STRING "USB product string passed to Arduino-Pico")
set(DOOM_BOOT_PROBE_ONLY OFF CACHE BOOL "Build a minimal app_start/app_task0 smoke-test firmware")
set(DOOM_DUAL_CORE_COLUMNS "0" CACHE STRING "Enable queued wall-column rendering path for staged dual-core work")
set(DOOM_RENDER_ASYNC_PLANES "0" CACHE STRING "Render visplanes on core1 concurrently with core0 columns/sprites")
set(DOOM_VIDEO_SYNC_FLUSH "0" CACHE STRING "Diagnostic: flush the TFT synchronously on core0 instead of async on core1")
set(DOOM_TFT_PANEL "ili9341" CACHE STRING "TFT panel driver: ili9341 or st7796s (ST7796S is RP2350-only)")
set(DOOM_HIGHRES_SCENE "0" CACHE STRING "Render the 3D scene at the full TFT panel resolution (ILI9341 320x240, ST7796S 480x320)")
set(DOOM_SYS_CLOCK_KHZ "250000" CACHE STRING "System clock (kHz) applied at boot; the VS Code manifest should set the target-specific value")
set(JH_ILI9341_SPI_DEFAULT_HZ "41666666" CACHE STRING "Requested TFT SPI clock (Hz); actual = clk_peri / even divisor")
set(ARDUINO_RP2040_PLATFORM_PATH "$ENV{HOME}/.arduino15/packages/rp2040/hardware/rp2040/5.4.0" CACHE PATH "Arduino-Pico platform path")
set(ARDUINO_TINYUSB_SRC "${ARDUINO_RP2040_PLATFORM_PATH}/libraries/Adafruit_TinyUSB_Arduino/src" CACHE PATH "Adafruit TinyUSB source include path")
set(JH_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../libraries/JaszczurHAL" CACHE PATH "JaszczurHAL library root")

if(NOT EXISTS "${JH_ROOT}/library.properties")
    message(FATAL_ERROR "JaszczurHAL not found at JH_ROOT='${JH_ROOT}'")
endif()

set(SKETCH_DIR "${CMAKE_BINARY_DIR}/sketch/${APP_NAME}")
set(SKETCH_SRC_DIR "${SKETCH_DIR}/src")
set(GENERATED_DIR "${CMAKE_BINARY_DIR}/generated")
set(ARDUINO_BUILD_DIR "${CMAKE_CURRENT_SOURCE_DIR}/.build/arduino")
set(FIRMWARE_ELF "${CMAKE_CURRENT_SOURCE_DIR}/.build/firmware.elf")
set(FIRMWARE_BIN "${CMAKE_CURRENT_SOURCE_DIR}/.build/firmware.bin")
set(FIRMWARE_UF2 "${CMAKE_CURRENT_SOURCE_DIR}/.build/firmware.uf2")
set(FIRMWARE_MAP "${CMAKE_CURRENT_SOURCE_DIR}/.build/firmware.map")

file(MAKE_DIRECTORY "${SKETCH_DIR}" "${SKETCH_SRC_DIR}" "${GENERATED_DIR}" "${ARDUINO_BUILD_DIR}")

set(PACKAGE_NAME "Chocolate Doom")
set(PACKAGE_TARNAME "chocolate-doom")
set(PACKAGE_VERSION "3.0.0")
set(PACKAGE_STRING "${PACKAGE_NAME} ${PACKAGE_VERSION}")
set(PROGRAM_PREFIX "chocolate-")
set(HAVE_DIRENT_H 1)
set(HAVE_MMAP 1)
set(HAVE_DECL_STRCASECMP 1)
set(HAVE_DECL_STRNCASECMP 1)
configure_file("${CMAKE_CURRENT_SOURCE_DIR}/cmake/config.h.cin" "${GENERATED_DIR}/config.h")

file(WRITE "${SKETCH_DIR}/${APP_NAME}.ino"
    "// Auto-generated by CMake - do not edit or commit.\n"
    "// Real application code lives in src/jaszczurhal/doom_app.cpp.\n"
    "#include <Arduino.h>\n"
    "#if defined(USE_TINYUSB)\n"
    "#include <Adafruit_TinyUSB.h>\n"
    "#endif\n"
    "#ifndef DOOM_BOOT_PROBE_ONLY\n"
    "#define DOOM_BOOT_PROBE_ONLY 0\n"
    "#endif\n"
    "#if !DOOM_BOOT_PROBE_ONLY\n"
    "#include <JaszczurHAL.h>\n"
    "#include <hal/hal_app.h>\n"
    "#else\n"
    "extern \"C\" void app_start(void);\n"
    "extern \"C\" void app_task0(void);\n"
    "#endif\n"
    "\n"
    "void setup(void) {\n"
    "    app_start();\n"
    "}\n"
    "\n"
    "void loop(void) {\n"
    "    app_task0();\n"
    "}\n"
    "\n"
    "#if defined(HAL_ENABLE_APP_TASK1) && !DOOM_BOOT_PROBE_ONLY\n"
    "void loop1(void) {\n"
    "    app_task1();\n"
    "}\n"
    "#endif\n"
)

function(link_into_sketch REL_PATH)
    set(_src "${CMAKE_CURRENT_SOURCE_DIR}/${REL_PATH}")
    set(_dst "${SKETCH_SRC_DIR}/${REL_PATH}")
    if(NOT EXISTS "${_src}")
        message(FATAL_ERROR "Missing source file: ${_src}")
    endif()
    get_filename_component(_dst_dir "${_dst}" DIRECTORY)
    file(MAKE_DIRECTORY "${_dst_dir}")
    file(REMOVE "${_dst}")
    file(CREATE_LINK "${_src}" "${_dst}" SYMBOLIC COPY_ON_ERROR)
endfunction()

file(REMOVE "${SKETCH_DIR}/hal_project_config.h")
file(CREATE_LINK
    "${CMAKE_CURRENT_SOURCE_DIR}/hal_project_config.h"
    "${SKETCH_DIR}/hal_project_config.h"
    SYMBOLIC COPY_ON_ERROR
)
file(REMOVE "${SKETCH_DIR}/doom_main_config.h")
file(CREATE_LINK
    "${CMAKE_CURRENT_SOURCE_DIR}/doom_main_config.h"
    "${SKETCH_DIR}/doom_main_config.h"
    SYMBOLIC COPY_ON_ERROR
)

if(DOOM_BOOT_PROBE_ONLY)
    set(DOOM_PORT_SOURCES
        src/jaszczurhal/doom_boot_probe.cpp
    )
else()
    file(GLOB DOOM_GAME_SOURCES CONFIGURE_DEPENDS
        "${CMAKE_CURRENT_SOURCE_DIR}/src/doom/*.c"
    )

    set(DOOM_PORT_SOURCES
        src/jaszczurhal/doom_app.cpp
        src/jaszczurhal/doom_backend_stub.c
        src/jaszczurhal/doom_input_hal.c
        src/jaszczurhal/doom_storage_hal.c
        src/jaszczurhal/doom_string_hal.c
        src/jaszczurhal/doom_timer_hal.c
        src/jaszczurhal/doom_video_hal.c

        src/aes_prng.c
        src/d_event.c
        src/d_iwad.c
        src/d_loop.c
        src/d_mode.c
        src/deh_str.c
        src/image_decoder.c
        src/i_sound.c
        src/i_main.c
        src/m_argv.c
        src/m_bbox.c
        src/m_cheat.c
        src/m_controls.c
        src/m_fixed.c
        src/m_misc.c
        src/memio.c
        src/musx_decoder.c
        src/sha1.c
        src/tables.c
        src/tiny_huff.c
        src/v_diskicon.c
        src/v_video.c
        src/w_checksum.c
        src/w_file.c
        src/w_file_memory.c
        src/w_main.c
        src/w_merge.c
        src/w_wad.c
        src/z_zone.c

        src/jaszczurhal/doom_glob_hal.c
        src/jaszczurhal/doom_sound_hal.c
        src/jaszczurhal/doom_system_hal.c
    )

    foreach(_src IN LISTS DOOM_GAME_SOURCES)
        file(RELATIVE_PATH _rel "${CMAKE_CURRENT_SOURCE_DIR}" "${_src}")
        list(APPEND DOOM_PORT_SOURCES "${_rel}")
    endforeach()
endif()

list(REMOVE_DUPLICATES DOOM_PORT_SOURCES)

file(REMOVE_RECURSE "${SKETCH_SRC_DIR}")
file(MAKE_DIRECTORY "${SKETCH_SRC_DIR}")

foreach(_src IN LISTS DOOM_PORT_SOURCES)
    link_into_sketch("${_src}")
endforeach()

if(DOOM_BOOT_PROBE_ONLY)
    set(DOOM_BOOT_PROBE_DEFINE DOOM_BOOT_PROBE_ONLY=1)
else()
    set(DOOM_BOOT_PROBE_DEFINE DOOM_BOOT_PROBE_ONLY=0)
endif()

foreach(_flag DOOM_DUAL_CORE_COLUMNS DOOM_RENDER_ASYNC_PLANES DOOM_VIDEO_SYNC_FLUSH)
    if(NOT ${_flag} MATCHES "^[01]$")
        message(FATAL_ERROR "${_flag} must be 0 or 1 (got '${${_flag}}')")
    endif()
endforeach()

if(NOT DOOM_HIGHRES_SCENE MATCHES "^[01]$")
    message(FATAL_ERROR "DOOM_HIGHRES_SCENE must be 0 or 1 (got '${DOOM_HIGHRES_SCENE}')")
endif()

string(TOLOWER "${DOOM_TFT_PANEL}" DOOM_TFT_PANEL_NORMALIZED)
if(NOT DOOM_TFT_PANEL_NORMALIZED MATCHES "^(ili9341|st7796s)$")
    message(FATAL_ERROR "DOOM_TFT_PANEL must be 'ili9341' or 'st7796s' (got '${DOOM_TFT_PANEL}')")
endif()

set(DOOM_TARGET_IS_RP2350 OFF)
if(ARDUINO_FQBN MATCHES "pico2|rp2350")
    set(DOOM_TARGET_IS_RP2350 ON)
endif()

set(DOOM_TFT_DEFINES)
if(DOOM_TFT_PANEL_NORMALIZED STREQUAL "st7796s")
    if(NOT DOOM_TARGET_IS_RP2350)
        message(FATAL_ERROR "DOOM_TFT_PANEL=st7796s is supported only for RP2350/Pico 2 builds")
    endif()
    list(APPEND DOOM_TFT_DEFINES
        DOOM_TFT_PANEL_ST7796S=1
        HAL_ENABLE_ST7796S
        HAL_DISPLAY_ST7796S
    )
else()
    list(APPEND DOOM_TFT_DEFINES
        DOOM_TFT_PANEL_ILI9341=1
        HAL_ENABLE_ILI9341
        HAL_DISPLAY_ILI9341
    )
endif()

set(DOOM_SCREEN_DEFINES)
if(DOOM_HIGHRES_SCENE STREQUAL "1")
    if(DOOM_TFT_PANEL_NORMALIZED STREQUAL "st7796s")
        if(NOT DOOM_TARGET_IS_RP2350)
            message(FATAL_ERROR "DOOM_HIGHRES_SCENE=1 with ST7796S is supported only for RP2350/Pico 2 builds")
        endif()
        list(APPEND DOOM_SCREEN_DEFINES
            DOOM_HIGHRES_SCENE=1
            SCREENWIDTH=480
            SCREENHEIGHT=320
            FIXED_SCREENWIDTH=0
        )
    else()
        list(APPEND DOOM_SCREEN_DEFINES
            DOOM_HIGHRES_SCENE=1
            SCREENWIDTH=320
            SCREENHEIGHT=240
            FIXED_SCREENWIDTH=1
        )
    endif()
else()
    list(APPEND DOOM_SCREEN_DEFINES
        DOOM_HIGHRES_SCENE=0
        SCREENWIDTH=320
        SCREENHEIGHT=200
        FIXED_SCREENWIDTH=1
    )
endif()

set(DOOM_PORT_DEFINES
    ${DOOM_TFT_DEFINES}
    ${DOOM_SCREEN_DEFINES}
    ${DOOM_BOOT_PROBE_DEFINE}
    DOOM_ONLY=1
    DOOM_SMALL=1
    DOOM_TINY=1
    DOOM_CONST=1
    PICO_BUILD=1
    PICO_DOOM=1
    LIB_PICO_STDLIB=1
    JASZCZURHAL_PORT=1
    JASZCZURHAL_ZONE_BYTES=65536
    JASZCZURHAL_ZONE_MIN_BYTES=49152
    DOOM_RENDER_ASYNC_PLANES=${DOOM_RENDER_ASYNC_PLANES}
    DOOM_VIDEO_SYNC_FLUSH=${DOOM_VIDEO_SYNC_FLUSH}
    DOOM_DUAL_CORE_COLUMNS=${DOOM_DUAL_CORE_COLUMNS}
    DOOM_SYS_CLOCK_KHZ=${DOOM_SYS_CLOCK_KHZ}
    DOOM_SPRITE_OCCLUSION=0
    JH_ILI9341_SPI_DEFAULT_HZ=${JH_ILI9341_SPI_DEFAULT_HZ}
    JH_ST77XX_SPI_DEFAULT_HZ=${JH_ILI9341_SPI_DEFAULT_HZ}
    SHRINK_MOBJ=1
    FLOOR_CEILING_CLIP_8BIT=0
    LOAD_COMPRESSED=1
    MUSX_COMPRESSED=1
    NO_DEMO_RECORDING=1
    NO_DRAW_BOTTOM=1
    NO_DRAW_MASKED=1
    NO_DRAW_MID=1
    NO_DRAW_PSPRITES=1
    NO_DRAWSEGS=0
    NO_DRAW_SKY=1
    NO_DRAW_SPRITES=0
    NO_DRAW_TOP=1
    NO_INTERCEPTS_OVERRUN=1
    NO_IERROR=1
    NO_MASKED_FLOOR_CLIP=0
    NO_FILE_ACCESS=1
    NO_RDRAW=1
    NO_SCREENSHOT=1
    NO_USE_ARGS=1
    NO_USE_BOUND_CONFIG=1
    NO_USE_CHECKSUM=1
    NO_USE_DEH=1
    NO_USE_EXIT=1
    NO_USE_FLOAT=1
    NO_USE_GUS=1
    NO_USE_JOYSTICK=1
    NO_USE_LIBSAMPLERATE=1
    NO_USE_LOAD=1
    NO_USE_LOADING_DISK=1
    NO_USE_MUSIC_PACKS=1
    NO_USE_MOUSE=1
    NO_USE_NET=1
    NO_USE_RELOAD=1
    NO_USE_SAVE=1
    NO_USE_SAVE_CONFIG=1
    NO_USE_STATE_MISC=1
    NO_USE_TIMIDITY=1
    NO_USE_WIPE=1
    NO_USE_ZLIGHT=1
    NO_VISPLANE_CACHES=1
    NO_VISPLANE_GUTS=1
    NO_VISSPRITES=0
    MAXVISSPRITES=32
    NO_Z_MALLOC_USER_PTR=1
    NO_Z_ZONE_ID=1
    PD_CLIP_WALLS=1
    PD_COLUMNS=1
    PD_DRAW_COLUMNS=1
    PD_DRAW_MARKERS=1
    PD_DRAW_PLANES=1
    PD_QUANTIZE=1
    PD_SCALE_SORT=1
    PD_SANITY=1
    PICO_NO_TIMING_DEMO=1
    SAVE_COMPRESSED=1
    TEMP_IMMUTABLE_DISABLED=1
    DOOM_FLASH_XIP_BASE=0x10000000
    DOOM_FLASH_SIZE_BYTES=0x400000
    DOOM_WHD_FLASH_ADDR=0x10200000
    TINY_WAD_ADDR=DOOM_WHD_FLASH_ADDR
    USE_ERASE_FRAME=1
    USE_CONST_MUSIC=1
    USE_CONST_SFX=1
    USE_FLAT_MAX_256=1
    USE_INDEX_LINEBUFFER=1
    USE_LIGHTMAP_INDEXES=1
    USE_MEMMAP_ONLY=1
    USE_MEMORY_WAD=1
    USE_MUSX=1
    USE_RAW_MAPLINEDEF=1
    USE_RAW_MAPNODE=1
    USE_RAW_MAPSEG=1
    USE_RAW_MAPTHING=1
    USE_RAW_MAPVERTEX=1
    USE_READONLY_MMAP=1
    USE_SINGLE_IWAD=1
    USE_THINKER_POOL=1
    USE_WHD=1
    WHD_SUPER_TINY=1
    USE_ZONE_FOR_MALLOC=1
    Z_MALOOC_EXTRA_DATA=1
    NUM_SOUND_CHANNELS=8
    SOUND_LOW_PASS=1
)

if(ARDUINO_FQBN MATCHES "rpipico2?w")
    list(APPEND DOOM_PORT_DEFINES
        PICO_CYW43_SUPPORTED=1
        CYW43_PIN_WL_DYNAMIC=1
    )
endif()

set(DOOM_PORT_INCLUDE_FLAGS
    "-I${SKETCH_DIR}"
    "-I${GENERATED_DIR}"
    "-I${CMAKE_CURRENT_SOURCE_DIR}/src"
    "-I${CMAKE_CURRENT_SOURCE_DIR}/src/doom"
    "-I${CMAKE_CURRENT_SOURCE_DIR}/src/jaszczurhal"
    "-I${CMAKE_CURRENT_SOURCE_DIR}/src/adpcm-xq"
)

set(DOOM_PORT_WARNING_FLAGS
    "-Wno-unused-variable"
    "-Wno-unused-function"
    "-Wno-unused-but-set-variable"
    "-Wno-unused-but-set-parameter"
    "-Wno-implicit-fallthrough"
    "-Wno-sign-compare"
    "-Wno-format-truncation"
    "-Wno-cpp"
    "-Wno-cast-function-type"
    "-Wno-maybe-uninitialized"
)

set(DOOM_PORT_C_WARNING_FLAGS
    ${DOOM_PORT_WARNING_FLAGS}
    "-Wno-discarded-qualifiers"
)

set(DOOM_PORT_DEFINE_FLAGS)
foreach(_def IN LISTS DOOM_PORT_DEFINES)
    list(APPEND DOOM_PORT_DEFINE_FLAGS "-D${_def}")
endforeach()

set(DOOM_PORT_C_COMPILER_FLAGS
    ${DOOM_PORT_INCLUDE_FLAGS}
    ${DOOM_PORT_C_WARNING_FLAGS}
    ${DOOM_PORT_DEFINE_FLAGS}
)

set(DOOM_PORT_CPP_COMPILER_FLAGS
    ${DOOM_PORT_INCLUDE_FLAGS}
    ${DOOM_PORT_WARNING_FLAGS}
    ${DOOM_PORT_DEFINE_FLAGS}
)

string(JOIN " " DOOM_PORT_C_COMPILER_FLAGS_STR ${DOOM_PORT_C_COMPILER_FLAGS})
string(JOIN " " DOOM_PORT_CPP_COMPILER_FLAGS_STR ${DOOM_PORT_CPP_COMPILER_FLAGS})

set(ARDUINO_LIBRARY_ARGS)
set(_sketchbook_libraries "")
set(_jh_in_sketchbook OFF)

if(ARDUINO_SKETCHBOOK)
    set(_sketchbook_libraries "${ARDUINO_SKETCHBOOK}/libraries")
    if(EXISTS "${_sketchbook_libraries}")
        list(APPEND ARDUINO_LIBRARY_ARGS --libraries "${_sketchbook_libraries}")
        if(EXISTS "${_sketchbook_libraries}/JaszczurHAL/library.properties")
            file(REAL_PATH "${JH_ROOT}" _jh_root_real)
            file(REAL_PATH "${_sketchbook_libraries}/JaszczurHAL" _jh_sketchbook_real)
            if(_jh_root_real STREQUAL _jh_sketchbook_real)
                set(_jh_in_sketchbook ON)
            endif()
        endif()
    endif()
endif()

if(NOT _jh_in_sketchbook)
    list(APPEND ARDUINO_LIBRARY_ARGS --library "${JH_ROOT}")
endif()

set(ARDUINO_COMMON_ARGS
    compile
    --fqbn "${ARDUINO_FQBN}"
    --build-path "${ARDUINO_BUILD_DIR}"
    ${ARDUINO_LIBRARY_ARGS}
    --build-property "compiler.cpp.extra_flags=${DOOM_PORT_CPP_COMPILER_FLAGS_STR}"
    --build-property "compiler.c.extra_flags=${DOOM_PORT_C_COMPILER_FLAGS_STR}"
    --warnings all
)

if(JH_USB_MANUFACTURER AND JH_USB_PRODUCT)
    list(APPEND ARDUINO_COMMON_ARGS
        --build-property "build.usb_manufacturer=\"${JH_USB_MANUFACTURER}\""
        --build-property "build.usb_product=\"${JH_USB_PRODUCT}\""
    )
endif()

if(ARDUINO_VERBOSE)
    list(APPEND ARDUINO_COMMON_ARGS --verbose)
endif()

function(add_arduino_compile_target TARGET_NAME)
    set(options DEBUG UPLOAD COMPILE_DB)
    cmake_parse_arguments(ARG "${options}" "" "" ${ARGN})

    set(_cmd "${ARDUINO_CLI}" ${ARDUINO_COMMON_ARGS})

    if(ARG_DEBUG)
        list(APPEND _cmd --optimize-for-debug)
    endif()

    if(ARG_COMPILE_DB)
        list(APPEND _cmd --only-compilation-database)
    endif()

    if(ARG_UPLOAD)
        list(APPEND _cmd --upload)
        if(ARDUINO_UPLOAD_PORT)
            list(APPEND _cmd --port "${ARDUINO_UPLOAD_PORT}")
        endif()
    endif()

    list(APPEND _cmd "${SKETCH_DIR}")

    add_custom_target("${TARGET_NAME}"
        COMMAND ${_cmd}
        WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
        USES_TERMINAL
        VERBATIM
    )

    if(NOT ARG_COMPILE_DB)
        add_custom_command(TARGET "${TARGET_NAME}" POST_BUILD
            COMMAND "${CMAKE_COMMAND}" -E copy_if_different
                "${ARDUINO_BUILD_DIR}/${APP_NAME}.ino.elf" "${FIRMWARE_ELF}"
            COMMAND "${CMAKE_COMMAND}" -E copy_if_different
                "${ARDUINO_BUILD_DIR}/${APP_NAME}.ino.bin" "${FIRMWARE_BIN}"
            COMMAND "${CMAKE_COMMAND}" -E copy_if_different
                "${ARDUINO_BUILD_DIR}/${APP_NAME}.ino.uf2" "${FIRMWARE_UF2}"
            COMMAND "${CMAKE_COMMAND}" -E copy_if_different
                "${ARDUINO_BUILD_DIR}/${APP_NAME}.ino.map" "${FIRMWARE_MAP}"
            VERBATIM
        )
    endif()
endfunction()

add_arduino_compile_target(firmware)
add_arduino_compile_target(firmware_debug DEBUG)
add_arduino_compile_target(firmware_upload UPLOAD)
add_arduino_compile_target(firmware_compile_db COMPILE_DB)

message(STATUS "Arduino FQBN: ${ARDUINO_FQBN}")
message(STATUS "Generated sketch: ${SKETCH_DIR}")
message(STATUS "Arduino build dir: ${ARDUINO_BUILD_DIR}")
message(STATUS "Arduino TinyUSB src: ${ARDUINO_TINYUSB_SRC}")
message(STATUS "Doom port source files: ${DOOM_PORT_SOURCES}")
